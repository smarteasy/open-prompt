# 🍌 NanoBanana Prompt Template (V3.0 Optimized)

This template is strictly optimized for the NANO-RENDER-V3 engine to generate high-quality, structured React Component code based on your visual data. **Fill in all bracketed placeholders `[ ]`** before execution.

## 0\. AI Service Configuration (NANO.CONFIG)

```markdown
# AI Service Configuration (NANO.CONFIG)
- Target Model: NANO-RENDER-V3 (Optimization Model)
- Resolution: [Width] x [Height] (e.g., 1920x1080)
- Output Format: React Component (Mandatory)
- Animation Requirement: [None / NANO.FADE_IN / NANO.HOVER_PULSE / Custom: (Describe)]
- Language: [Korean / English]
```

## 1\. Role and Goal (NANO.GOAL)

```markdown
# Role and Goal (NANO.GOAL)
You are a professional '[Specific Role Name]' for the NANO-RENDER-V3 engine.
Based on the [Theme Name] data provided below, generate a [Adjective for Purpose] and **component-reusable** '[Type of Output]' React Component code.
```

## 2\. Visual Style and Design Tokens (NANO.TOKENS)

```markdown
# Visual Style and Design Tokens (NANO.TOKENS)
1. Style: [Detailed style description - texture, outline treatment, etc.]
2. Layout: [Arrangement method and CSS Flex/Grid instructions]
3. **Design Tokens (Variables):**
   - COLOR_PRIMARY: [#HEX_CODE, Purpose (e.g., Main Accent)]
   - COLOR_SECONDARY: [#HEX_CODE, Purpose (e.g., Background)]
   - FONT_MAIN: [Font Style (e.g., Inter, Semibold, Size: 20px)]
   - SHADOW_DEPTH: [CSS Shadow Property Value]
4. Text Rendering: All text within the image must be in '[Language]' using **UTF-8 encoding**.
5. Rendering Instruction: All elements must be **grouped with `<g>` tags**, strictly adhering to `z-index` hierarchy.
```

## 3\. Component Structure and Hierarchy (NANO.STRUCTURE)

```markdown
# Component Structure and Hierarchy (NANO.STRUCTURE)
- Overall View Box: [Width x Height Ratio]
- Component Layers (Z-index): 
  - `<NANO_BG_LAYER>` (Z-index: 0): [Background/Pattern handling]
  - `<NANO_CONTENT_LAYER>` (Z-index: 1): [Main content handling]
  - `<NANO_OVERLAY_LAYER>` (Z-index: 2): [Emphasis/Tooltip/Animation element handling]
- Flow Direction: [Reading order and connection line direction (e.g., Left-to-Right Timeline)]
```

## 4\. Required Element Function Calls (NANO.CALLS)

```markdown
# Required Element Function Calls (NANO.CALLS)
For each [Unit Element], the following NanoBanana internal functions must be called:
1. Icon/Symbol: **NANO_ELEMENT_CALL(IconID: [ID], Color: COLOR_PRIMARY, Size: [px])**
2. Text: **NANO_TEXT_CALL("[Text Content]", Font: FONT_MAIN, Position: [x, y])**
3. Graphic/Illustration: **NANO_PATH_CALL(PathData: [Simple shape description], Fill: COLOR_SECONDARY)**
4. Data Validation Check: Main content items must be **at least [Minimum Number, e.g., 3]** items.
```

## 5\. Input Data Structure (Mandatory JSON Format)

```json
# Input Data: [Theme Name] - Mandatory JSON Format

{
  "header": {
    "title": "[Main Title]",
    "subtitle": "[Sub Title]"
  },
  "steps": [
    {
      "id": 1,
      "label": "[First Item]",
      "visual_asset": "[NANO Asset ID or detailed visual description]",
      "text_data": "[Text content to display]",
      "step_color": "[#HEX_CODE or COLOR_PRIMARY/SECONDARY]"
    },
    // ... remaining items ...
  ],
  "footer": {
    "closing_text": "[Closing element]"
  }
}
```

## 6\. Decoration and Atmosphere Elements (NANO.DECORATION\_PROPS)

```markdown
# Decoration and Atmosphere Elements (NANO.DECORATION_PROPS)
- Background Pattern: [Wave, Geometric, Gradient, etc.]
- Connection Element: [Arrows, Dashed lines, Ribbons, etc.]
  - **STYLE_PROP**: stroke-width: [px], stroke-dasharray: [value]
- Emphasis Effect: [Shadows, Borders, Highlights, etc.]
  - **STYLE_PROP**: filter: url(#ShadowFilter), opacity: [value]
```

## 7\. Final Output Request and Specification (NANO.OUTPUT)

```markdown
# Final Output Request and Specification (NANO.OUTPUT)
Follow all instructions above and output a **single React Functional Component code block** without errors.
- Component Name: Nano[ThemeName]Chart
- **Special Emphasis**: All text must use the defined font token (FONT_MAIN).
- The final output must perfectly convey the [Final Mood/Feeling].
```

## 8\. Debugging and Error Handling Instructions (NANO.ERROR\_HANDLER)

```json
# Debugging and Error Handling Instructions (NANO.ERROR_HANDLER)
If required data is missing or validation (item 4) fails, output the following JSON error message instead of the result:
{
  "error_code": "NANO-E-003",
  "message": "Required steps/data missing or invalid.",
  "guidance": "Main content (steps) requires at least [Number] items. Please verify the JSON structure."
}
```
